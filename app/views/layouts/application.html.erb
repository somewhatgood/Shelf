<!DOCTYPE html>
<html>
<head>
  <title>Shelf</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <!-- ログイン・ログアウト　-->
  <% if user_signed_in? %>
		<%# Deviseでログインした場合 %>
	    <%= current_user.omniuser.name %>さん
	    (DEVISEでログイン)omniuserIDは<%= current_user.omniuser_id %>さん
	    <%= link_to "ログアウト", destroy_user_session_path, :method => :delete %>
  <% elsif current_omniuser %>
  		<%# Omniuserでログインした場合 %>
       	<%= current_omniuser.name %>さん
       	omniuserIDは<%= current_omniuser.id %>さん
       	<%= link_to "ログアウト", logout_path %>
  <% else %>
  <% #どちらでもログインしていなければ「ログイン」を表示。クリックするとログインページへ遷移させる。%>
        <%= link_to "メンバー登録", welcome_path %><br/>
        <%= link_to "ログイン", new_user_session_path %>
  <% end %>
  <!-- /ログイン・ログアウト-->



		<p id="notice"><%= notice %></p>
		<p id="notice"><%= alert %></p>
		


<%= yield %>

</body>
</html>
