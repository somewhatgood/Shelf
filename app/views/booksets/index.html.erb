<h1>Listing booksets</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Category</th>
    <th>Image url</th>
    <th>Omniuser_name</th>
    <th>Apply</th>
    <th>Reply</th>
    <th></th>
  </tr>

<% @booksets.each do |bookset| %>
  <tr>
    <td><%= bookset.description %></td>
    <td><%= bookset.category %></td>
    <td><%= bookset.image %></td>
    <td><%= bookset.omniuser.name %></td>
    <td><%#= bookset.applies.exists? ? link_to("アプローチあり", bookset) : "アプローチなし" %></td> <!-- 【申請あり】のリンク先をSHOWにしてある -->
    <td><%#= bookset.replies.exists? ? link_to("成立チャンス", bookset) : "返信なし" %></td> <!-- 【申請あり】のリンク先をSHOWにしてある -->
    <td><%#= bookset.applies.exists? ? "×" : (link_to 'Edit', edit_bookset_path(bookset)) %></td>
    <td><%#= bookset.applies.exists? ? "×" : (link_to 'Destroy', bookset, confirm: 'Are you sure?', method: :delete) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bookset', new_bookset_path %>
